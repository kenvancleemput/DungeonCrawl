public class Weapon extends Item {

    public Weapon(String description, double weight, String name, boolean isEquippable, boolean isConsumable, int attackBonus, int defenseBonus, int damageBonus) {
        super(description, weight, name, isEquippable, isConsumable, attackBonus, defenseBonus, damageBonus);
    }
}
